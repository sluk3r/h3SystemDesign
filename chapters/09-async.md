# 第 9 章：异步化——让等待消失

> **所属部**：第三部 · 高性能——毫秒必争 | **状态**：🔲 待写
>
> **叙事设计**：异步化这一章的核心张力在于线程模型的设计选择。从“开线程池就完事了”的朴素想法，到理解上下文切换的代价，再到协程/虚拟线程，是一个逐渐深入的过程。事件驱动架构放在最后作为终极形态，但不强推——强调适用边界。

---

## 9.1 场景：下单后要发短信、扣积分、写流水……用户等不及了

> **写作提示**：一个下单请求背后有多少同步操作。全串行 vs 全并行的两难。

- [ ] 下单请求的负担
- [ ] 串行 vs 并行的抉择

## 9.2 从同步到异步：线程池不是银弹

> **写作提示**：`newFixedThreadPool(200)` ——然后 OOM。线程上下文切换的成本不是免费午餐。协程/虚拟线程的思想。

- [ ] 线程池的陷阱
- [ ] 上下文切换的成本
- [ ] 协程与虚拟线程

## 9.3 消息队列：削峰填谷之外的故事

> **写作提示**：解耦——服务间的“邮局”。顺序消息。死信队列——失败的消息不是垃圾，是线索。

- [ ] 消息队列的“邮局”模型
- [ ] 顺序消息
- [ ] 死信队列的侦探价值

## 9.4 事件驱动架构：因果一致性的武器

> **写作提示**：从“订单创建后通知下游”到“订单已创建”事件的广播。Event Sourcing + CQRS 的极端方案。适用边界。

- [ ] 从通知到事件
- [ ] Event Sourcing + CQRS
- [ ] 适用边界：不是所有系统都值得

## 9.5 原理沉淀：Little 定律与吞吐量

> **写作提示**：要提升吞吐（λ），降低延迟（W）或增加并发（L）。异步化本质上是“在等待 I/O 时让出 CPU 去干别的活”。

- [ ] Little 定律的吞吐量视角
- [ ] 异步化的本质
